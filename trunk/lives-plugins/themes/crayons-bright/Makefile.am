## Process this with automake to create Makefile.in
THEME_NAME=crayons-bright

themesdir = "$(datadir)/$(PACKAGE)/themes/$(THEME_NAME)"
themes_DATA = \
frame.jpg \
main.jpg \
header.theme

EXTRA_DIST = $(themes_DATA)
